# Revision 7 live resource audit (2026-08-24)

Audit window: 2026-08-24 11:04:50–11:07:34 CST
Mode: read-only. No directory was created, no artifact was transferred, and no
inference, engine build, service, or process intervention was performed.

## 5090-zem

- Host: `zem-pc`; user `zem`
- GPU: NVIDIA GeForce RTX 5090 D v2; 15 MiB used / 24,455 MiB total; 0%
  utilization; 41 C; about 26–28 W
- Compute applications: none
- Matching Wall-OSS/pi.cpp/DStereo/TensorRT processes and containers: none
- Project filesystem free: `295,160,909,824` bytes
- Existing Rev5 and Rev6 directories remain; fresh Rev7 directory is absent:
  `/home/zem/wujinlong/picpp_walloss_20260818/revision7_export`
- System Python has no `torch`, `onnx`, `onnxruntime`, `numpy`, or `ml_dtypes`;
  a task-local environment/package must be staged before reference execution.

Decision: **machine resource precondition passes**. The separate human
`revision7-5090-execution-approval` is still required.

## agx-test

- Host: `agx-test`; user `root`; Jetson AGX Orin Developer Kit/aarch64
- TensorRT `trtexec` v10.3 (`100300`)
- `GR3D_FREQ=0%`, `EMC_FREQ=0%`; no matching Wall-OSS/TensorRT/server task;
  only desktop/system processes were present
- NVMe free: `70,223,773,696` bytes
- Rootfs free: `3,911,143,424` bytes; forbidden for artifacts and scratch
- Fresh Rev7 engine directory is absent:
  `/root/wujinlong/nvme/picpp_walloss_20260818/revision7_engine`
- Existing Rev3 export remains and was not modified

Decision: **mechanically idle, but not build-eligible**. Candidate freeze and
the independent `agx-revision7-build-approval` must precede transfer/build.
